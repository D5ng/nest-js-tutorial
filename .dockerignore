# --------------------------------------------------------
# .dockerignore: Docker 빌드 시 "빌드 컨텍스트"에서 제외할 경로
# --------------------------------------------------------
# COPY . . 할 때 이 목록에 있는 건 서버로 안 보내고, 이미지 레이어에도 안 들어감.
# → 빌드 속도 up, 이미지 크기·보안 위험 down.

# ---------- 의존성 (컨테이너 안에서 npm ci 로 새로 설치함) ----------
node_modules
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# ---------- 빌드 결과물 (컨테이너 안에서 npm run build 로 생성함) ----------
dist
build

# ---------- 버전 관리 (이미지에 넣을 이유 없음, 용량만 큼) ----------
.git
.gitignore

# ---------- 환경 변수·비밀 (절대 이미지에 넣지 말 것) ----------
.env
.env.*
.env.local
.env.development.local
.env.test.local
.env.production.local

# ---------- 테스트·커버리지 (프로덕션 이미지 빌드에 불필요) ----------
coverage
.nyc_output
*.lcov

# ---------- IDE·에디터 설정 (실행/빌드와 무관) ----------
.vscode
.idea
*.sublime-workspace
.project
.classpath
.settings

# ---------- OS·임시·로그 ----------
.DS_Store
Thumbs.db
*.log
.temp
.tmp
pids
*.pid
*.seed

# ---------- 문서·Docker 메타 (이미지 안에서 쓸 일 없음) ----------
README.md
*.md
Dockerfile
docker-compose*.yml
docker-compose*.yaml
.dockerignore

# ---------- 선택: E2E 테스트 (이미지에서 테스트 안 돌리면 제외해도 됨) ----------
# test
