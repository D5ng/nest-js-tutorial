# =============================================================================
# .env.example — 환경 변수 템플릿 (이 파일을 복사해 .env 를 만들고 값을 채우세요)
# =============================================================================
# 주의: .env 는 git 에 올리지 말 것. .env.example 만 커밋.

# ---------- 앱 기본 ----------
# 서버가 리스닝할 포트 (main.ts 에서 process.env.PORT 사용, 기본값 8080)
PORT=8080

# 실행 환경: development | production | test
# production 이면 로그/에러 노출 등이 제한되는 라이브러리 있음
NODE_ENV=development

# ---------- (나중에 DB 쓸 때) ----------
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# 또는
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=user
# DB_PASSWORD=password
# DB_DATABASE=dbname

# ---------- (나중에 외부 API·JWT 등 쓸 때) ----------
# API_KEY=your-api-key
# JWT_SECRET=your-jwt-secret
# JWT_EXPIRES_IN=7d
